services:
  # NestJS Backend API
  - type: web
    name: kenzo-hrms-api
    env: node
    plan: free
    buildCommand: npm install && npm run build -w apps/api
    startCommand: npm run start:prod -w apps/api
    envVars:
      - key: NODE_VERSION
        value: 20.14.0


